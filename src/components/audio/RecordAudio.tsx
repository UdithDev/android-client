import { SafeAreaView, View, Text } from "react-native";

const RecordAudio: React.FC = () => {
  return (
    <SafeAreaView>
      <View>
        <View>
          <Text>This is a RecordAudio screen</Text>
        </View>
      </View>
    </SafeAreaView>
  );
};

export default RecordAudio;
